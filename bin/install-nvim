#!/bin/bash

source platform

install_neovim () {
  if [[ "$PLATFORM" == "ubuntu"* ]]; then
    wget https://github.com/neovim/neovim/releases/latest/download/nvim-linux64.appimage
    chmod u+x nvim.appimage
    sudo mv nvim.appimage /usr/local/bin/nvim
  fi
}

install_neovim

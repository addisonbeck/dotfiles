#!/usr/bin/env bash

source dev-core

if [[ "$(hostname)" == "bw" ]]; then
  KEY="yknano"
fi

if [[ "$(hostname)" == "mac" ]]; then
  KEY="yknano"
fi

SESSION_PREFIX="📡"
SESSION_NAME="ssh → dev"
STARTER_COMMAND="ssh dev -i ~/.ssh/$KEY"
WINDOW_NAME="connect"

ensure_command_installed tmux
kill_session_if_exists
start_session

#!/usr/bin/env bash

if ! command -v tmux
then
  install-tmux
fi

if [[ "$(hostname)" == "bw" ]]; then
  KEY="yknano"
fi

if [[ "$(hostname)" == "mac" ]]; then
  KEY="yknano"
fi

tmux new-session -d -s ssh-dev -n 📡 "ssh dev -i ~/.ssh/$KEY"
tmux switch-client -t ssh-dev || (start-tmux "tmux switch-client -t ssh-dev")

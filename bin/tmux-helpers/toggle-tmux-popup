#!/usr/bin/env bash

if [ "$(tmux display-message -p -F "#{session_name}")" = "scratchpad" ];then
    tmux detach-client
else
    tmux popup -d '#{pane_current_path}' -xC -yC -w80% -h75% -E "tmux attach -t scratchpad || tmux new -s scratchpad"
fi
